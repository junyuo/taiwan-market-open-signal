{
  "name": "taiwan-market-open-signal",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "fetch:data": "node --import tsx scripts/fetch-market-data.ts",
    "collect:outcome": "node --import tsx scripts/collect-twse-outcome.ts",
    "validate:data": "node --import tsx scripts/validate-data.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^4.1.9"
  }
}
